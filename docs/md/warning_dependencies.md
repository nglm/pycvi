**Important note:** As of June 2026, the latest version of
`scikit-learn-extra` (0.3.0) is not compatible with `numpy>= 2.0.0`. If you
combine `scikit-learn-extra` with PyCVI, make sure that your NumPy version is
compatible with both packages.
